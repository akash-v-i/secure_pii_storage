
# SECURE-VAULT backend runtime dependencies
# Kept intentionally minimal - only what the app actually imports.
# Versions below are the ones verified working locally.

fastapi==0.109.0
starlette==0.35.1
uvicorn==0.27.0
python-multipart==0.0.32

# Config / env
python-dotenv==1.0.1

# Database
SQLAlchemy==2.0.51
PyMySQL==1.2.0

# Auth / crypto
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==46.0.5

# Validation / sanitisation
pydantic==2.12.5
email-validator==2.1.0.post1
bleach==6.1.0

# Rate limiting
slowapi==0.1.9

# Outbound HTTP (GeoIP lookup)
requests==2.31.0
